<% if @feed_items.any? %>

 <%= paginate @feed_items %>
 <%= page_entries_info @feed_items%>
 
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= paginate @feed_items %>
  <% end %>