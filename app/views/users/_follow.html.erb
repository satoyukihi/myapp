<%= form_with(model: current_user.following_relationships.build(following_id: @user.id)) do |f| %>
  <%= f.hidden_field :following_id %>
  <%= f.submit "フォローする", class: "btn btn-outline-secondary" %>
<% end %>