

<%= form_for(current_user.favorite_relationships.find_by(micropost_id: micropost.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= button_tag( class: "btn page-link text-red d-inline-block border border-0 iine") do %>
    <%= content_tag :span, micropost.liked_by.count, class: "fas fa-heart" %>
  <% end %>
<% end %>