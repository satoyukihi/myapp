
<% if logged_in? %>
<%= form_for(current_user.favorite_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :micropost_id, micropost.id %></div>
  <%= button_tag(class: "btn page-link tex-red d-inline-block border border-0 iine" ) do %>
    <%= content_tag :span, micropost.liked_by.count, class: "far fa-heart" %>
  <% end %>
<% end %>
<% end %>

<%= button_tag(class: "btn page-link tex-red d-inline-block border border-0 iine" ) do %>
    <%= link_to "#{micropost.liked_by.count}", login_path, class: "fas fa-heart" %>
  <% end %>