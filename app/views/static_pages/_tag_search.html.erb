<%= form_with url: root_path, method: :get, class: 'micropost_search' do %>
  <%= select_tag :tag_id,
    options_from_collection_for_select(Tag.all, :id, :name, params[:tag_id]),
    {
      prompt: 'タグ検索',
      class: 'form-control tag_select',
      onchange: 'submit(this.form);'
    }%>
<% end %>