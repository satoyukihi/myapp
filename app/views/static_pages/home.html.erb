<% content_for(:html_title) { 'Myごはん' } %>
<main role="main" class="container">
  
  <div class="home">
      <h1>ようこそMyごはんへ。</h1>
      <h5>みんなと料理をシェアしよう。</h5>
      <% if !logged_in? %>
      <p><%= link_to "Myごはんを始める>", " signup", class:"btn" %></p>
      <% end %>
  </div>
</main>
  
  <div class="microposts">
      <% if@microposts.any? %>
      
         <ol class="microposts">
            <%= link_to "新規投稿",new_micropost_path , class:"btn page-link text-dark d-inline-block" %></p>
            <%= paginate @microposts %>
            <%= page_entries_info @microposts%>
            <%= render @microposts%>
            <%= paginate @microposts %>
         </ol>
         
         
      <% end %>
      
  </div>

