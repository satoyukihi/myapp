<main role="main" class="container">
   <% if logged_in? %>
    <div class="col-md-8">
      <h3>投稿一覧</h3>
      <%= render 'shared/feed' %>
    </div>
   <% else %>
   
  <div class="home">
      <h1>ようこそMyごはんへ。</h1>
      <h5>毎日の料理を投稿＆探そう‼</h5>
      <p><%= link_to "Myごはんを始める>", " signup", class:"btn" %></p>
  </div>
</main>
<% end %>